{
  "name": "files2file_json",
  "version": "1.0.0",
  "description": "This function uses fs readFile & fs readdir and Asynchronously reads a folder of JSON files and writes then into a single JSON file in that directory",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
